projects:
  app:
    schemaPath: "server/src/schema.graphql"
    extensions:
      endpoints:
        default: "http://localhost:4000"
  database:
    schemaPath: "server/src/generated/graphcool.graphql"
    extensions:
      graphcool: server/database/graphcool.yml
